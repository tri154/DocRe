code cua ho:
batch_size = 4
step = batch_id
gradient_accumulation_steps = 4 (sau bao nhieu batch thi call step)
evaluation_steps = 500 (sau bao nhieu batch thi call evaluate)
Code co the loi phan: (step + 1) == len(train_dataloader) - 1


code cua minh:
batch_size = 4
idx_batch = batch_id
update_freq = 1 (sau bao nhieu batch thi call step)
evaluation_step = ? (sau bao nhieu batch thi call evaluate)


idx_batch % update_freq == 0
idx_batch % evaluation_freq == 0
so ixd_batch == CM(update_freq, evaluation_freq)
LCM(500 ,4) = 500
number of batch > LCM(update_freq, evaluation_freq)
