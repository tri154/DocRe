cu de cho no train trong 1 epoch (khong dung import loss, khong dung gate loss), sau do thi frezze
tat ca weight ngoai tru weight cua gate (su dung import loss )


warmup:
1 epoch dau
add noise, co import loss, khong gate loss, ko gate freeze.

fit data:
1 epoch
frezze gate lai, khong noise, khong import loss, khong gate loss.

1 epoch
re-routing:
freeze tat ca ngoai tru gate, khong noise, khong import loss, co gate loss.


epoch = 0:
	warmup:
	toan bo nhan gradient

	rerouting:
	moi gate nhan gradient


epoch != 0:
	fitting:
	ngoai tru gate nhan gradient

	rerouting:
	moi gate nhan gradient


Ket qua:
Nhin chung voi cac seed khac nhau thi phan bo train da hoi tu.

Van de:
- epoch dau chua can bang lam, van bi lech (co pattern giua cac seed)
- pattern: [4, 1, 0, 0], 2 experts con lai khong duoc dung
- diem khac biet lan chay dc diem cao vs diem thap:
+ diem cao: loss thap hon (1.2)
+ diem thap: loss cao hon (1.5)
--> co ve la do hen xui, khong thay pattern dac biet nao khac.

Thu nghiem 2:
voi experst = 2 thi no don vao chi 1 expert.
--> co the phai lam de can bang epoch warmup hon
Mot so seed van cho ra pattern (4, 1)

Huong tiep theo:
- lam the nao de cho can bang epoch warmup:
	+ tang importance_weight.
	+ tang epsilon o noise.
	+ tang so luong epoch warmup
- co nen rerouting trong warmup phase ?
- them regulalization trong rerouting phase ?

Tat importance los strong warmup phase:
- gate dung yen khong thay doi, train chi thuoc vao noise.

# CDR bi van de:
- tang epoch thi dev tang, nhung test lai giam
# co the GDA khong bi van de nay.


# Van de 1: Van de sau khi thu lam can bang hon:
- o nhung epoch dau (warmup) thi du lieu chua duoc fit, nen nhung expert nao nhan nhieu sample (random) thi sau khi rerouting thuong se lay mat cac sample cua expert khac.
VD: epoch 2/15 (warmup):
Stats train (before rerouting): tensor([1339., 1351., 1197., 1272.])
Stats train (after rerouting): tensor([3261.,  151.,   86., 1661.])
- gate bi overfitting.


# so sanh su khac biet khi tang importance weight ?
	khong co nhieu khac biet:
		- co the do ban dau init weight = zero nen CV cho ra output rat be (cac vi tri trong gate khong khac nhau nhieu (1.0 * uniform))
	--> thu nghiem: tat importance weight khi warmup.


Thu nghiem:

- Mot so seed van cho ra pattern (4, 1) voi n_experts = 2
- (2) lieu cai pattern la do 'Van de 1' gay ra hay la do no thuc su cover duoc pattern cua dataset.
-->
	- co nen rerouting trong warmup phase (nguyen nhan cua 'Van de 1')? (1)
- them regulalization trong rerouting phase ? (prevent gate overfitting)

Truong hop sau khi sua xong (1) ma no van cho ra pattern do thi:
	--> ung ho 'cover duoc pattern cua dataset'
Truong hop khong cho ra pattern thi:
	--> ung ho 'Van de 1'

Ket qua thu nghiem
- tang so luong epoch --> diem giam, van ch ra pattern (4, 1) ngoai ra xuat hien pattern khac nua, it. Co the la do dac diem cua CDR.
- thu nghiem: tat importance weight khi warmup --> co ve la importance weight o warmup phase khong co nhieu anh huong. (OK)
